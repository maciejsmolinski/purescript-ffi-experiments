{
  "dependencies": {
    "parcel": "^1.10.0"
  },
  "scripts": {
    "dev": "parcel serve src/index.html",
    "build": "parcel build src/index.html",
    "pulp": "pulp --watch build",
    "test": "pulp --watch test"
  }
}
